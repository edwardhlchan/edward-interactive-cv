# About Me Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the body of `about.md` with a concise, professional, evidence-grounded English About page for Edward Chan while preserving its front matter and both approved GitHub statistics image embeds with the username updated to `lmaodick1239`.

**Architecture:** Keep the existing Astro content contract intact: edit only the Markdown body after the closing front-matter delimiter in `about.md`. Organize the body as an English capability profile with identity and summary, grouped capabilities, selected work, and education/achievements; retain the two existing GitHub statistics image embeds as the sole amended legacy embeds, changing their username query values from `yusixian` to `lmaodick1239`.

**Tech Stack:** Markdown, YAML front matter, existing Astro content pipeline, POSIX shell verification commands (`git diff`, `awk`, `rg`, and a Markdown parser if already available).

## Global Constraints

- Modify only `about.md`; do not modify the approved design spec, resume PDF, writing guide, layout, site configuration, assets, or any other file.
- Preserve the complete front matter byte-for-byte, including `layout: ../layouts/PageLayout.astro`, `title: "About"`, `coverTitle: "关于我"`, `date: 2025-01-03 01:01:33`, `description: "关于我？"`, delimiter placement, and file type.
- The public identity is Edward Chan; `lmaodick1239` is the identity-matched GitHub username required by the user amendment for the preserved statistics embeds. Do not present the old `余弦`, `cosine`, or `yusixian` identity as Edward Chan.
- Write professional, direct English prose grounded in the authoritative resume; exclude unsupported identity, ownership, project, employment, credential, metric, deployment, impact, contact, and biographical claims.
- Follow the approved body order: identity and summary; capabilities; selected work; education and achievements; no contact/links section unless separately verified.
- Use only resume-supported technologies and capabilities: Python, C#, JavaScript, Splunk SIEM, event-log analysis, incident-response workflows, network fundamentals, PyQt, Pandas, Excel, data parsing, and data formatting.
- Include only evidenced selected work: Splunk BOTS security-log analysis and incident-response simulation; Python/PyQt data-parsing desktop application; C# real-time input-translation engine; and JavaScript/Cloudflare Workers serverless functions. Do not add repository URLs, deployment details, metrics, or user impact.
- Preserve both existing GitHub statistics image embeds in the body, changing only the relevant username values from `yusixian` to `lmaodick1239`; do not preserve the old GitHub badge, profile link, streak destination link, or other unverified `yusixian` associations.
- Keep the resulting body valid Markdown and concise enough for an About page rather than a full CV; do not add decorative quotes, marketing callouts, or layout work.

---

## Exact File Map

- Modify: `about.md` — replace only the Markdown body after line 7's closing front-matter delimiter; preserve front matter byte-for-byte; retain the two GitHub statistics image embeds and update their username query values.
- Read-only reference: `docs/superpowers/specs/2026-08-17-about-me-design.md` — approved content structure, evidence boundary, exclusions, and acceptance criteria.
- Read-only evidence: `Black and White Clean Professional A4 Resume-3.pdf` — authoritative identity, skills, project, education, and achievement evidence.
- Read-only guidance: `CV_Writing_Assistant_Guide.md` — presentation guidance only; its example claims are not evidence.
- Create: `docs/superpowers/plans/2026-08-17-about-me.md` — this implementation plan; it is created before implementation and is not part of the implementation change set.

## Implementation Steps

### Task 1: Capture the current file contract before editing

**Files:**
- Read: `about.md`
- Read: `docs/superpowers/specs/2026-08-17-about-me-design.md`
- Read: `Black and White Clean Professional A4 Resume-3.pdf`

- [ ] **Step 1: Record the exact front matter and embed lines.**

  Confirm that the implementation target starts with the existing seven-line front matter and that the body currently contains both statistics images: the `git-stats.cosine.ren` image with `username=yusixian` and the streak image with `user=yusixian`. Treat these two image syntaxes as the exact structures to preserve while changing only their username query values.

- [ ] **Step 2: Extract only resume-supported facts for the new prose.**

  Build the content from these approved facts: Edward Chan; focus on IT operations, cybersecurity, automation, programming, and data science; the named technical skills; Splunk BOTS analysis and Top 10 result in 2025; the Python/PyQt application; the C# input-remapping engine; JavaScript and Cloudflare Workers serverless functions; the Hong Kong Baptist University School of Continuing Education Higher Diploma in Data Science, CLAP-TECH Pathway, expected start date `2026-09-01`; Tech Basics / Secondary Education evidence; Distinction II; IBM P-TECH-aligned coursework; Equinix Data Center industry immersion; Level 5 English Language; Level 5 ICT; Level 3 Mathematics; and Canadian Computing Competition Distinction in 2025. Do not promote guide-only examples into claims.

### Task 2: Draft the English capability-profile body

**Files:**
- Modify: `about.md` body only, after the closing front-matter delimiter

- [ ] **Step 1: Add the identity and summary section.**

  Use a clear heading naming Edward Chan and the descriptor `IT Operations, Cybersecurity & Automation Assistant`. Follow it with a short professional English summary of the resume-supported technical focus. Do not mention `余弦`, `cosine`, `yusixian`, or any unverified account ownership.

- [ ] **Step 2: Add grouped, scannable capabilities.**

  Use headings and lists grouped under cybersecurity and IT operations; programming; and data, automation, tools, and cloud. Include only the approved technologies and capabilities, including Python, C#, JavaScript, Splunk SIEM, event-log analysis, incident-response workflows, network fundamentals, PyQt, Pandas, Excel, data parsing, and data formatting.

- [ ] **Step 3: Add the four selected-work entries.**

  Write concise, outcome-oriented entries for: Splunk BOTS security-log analysis and incident-response simulation; the Python/PyQt desktop application for data parsing, extraction, filtering, and formatting; the C# real-time controller-input remapping and emulation engine; and JavaScript/Cloudflare Workers serverless web functions. State only actions and outcomes supported by the resume. Do not invent metrics, repository links, screenshots, demos, download links, deployment outcomes, performance figures, or business impact.

- [ ] **Step 4: Add education and achievements.**

  State the Higher Diploma in Data Science association with Hong Kong Baptist University School of Continuing Education and the CLAP-TECH Pathway using the exact status `expected start date 2026-09-01`; do not call Edward currently enrolled or graduated. Include the supported Tech Basics / Secondary Education evidence, relevant results, Splunk BOTS Top 10 in 2025, and Canadian Computing Competition Distinction in 2025 where concise.

### Task 3: Preserve and amend the GitHub statistics embeds

**Files:**
- Modify: `about.md` body only

- [ ] **Step 1: Preserve the GitHub statistics image syntax.**

  Keep both existing image embeds in the resulting body: the GitHub state image endpoint and the GitHub streak image endpoint. Preserve their image URLs and alt text unless a syntax correction is required for valid Markdown; do not add the old GitHub badge or any old profile/streak destination link.

- [ ] **Step 2: Update both username query values.**

  Change the GitHub state image query from `username=yusixian` to `username=lmaodick1239` and change the streak image query from `user=yusixian` to `user=lmaodick1239`. The resulting body must contain exactly two GitHub statistics image embeds associated with `lmaodick1239`, with no `yusixian` occurrence.

- [ ] **Step 3: Keep the embed amendment separate from prose identity.**

  Treat `lmaodick1239` as the required identity-matched GitHub username for these two preserved images, while naming Edward Chan as the sole prose/page identity. Do not infer or add a GitHub profile destination, contact link, project link, or ownership statement beyond the amended image URLs.

### Task 4: Validate the completed Markdown change

**Files:**
- Verify: `about.md`

- [ ] **Step 1: Confirm only the permitted implementation file changed.**

  Run:

  ```bash
  git diff --name-only -- about.md docs/superpowers/specs/2026-08-17-about-me-design.md Black\ and\ White\ Clean\ Professional\ A4\ Resume-3.pdf CV_Writing_Assistant_Guide.md
  ```

  Expected: only `about.md` is listed for the implementation change. The plan file may also exist as the requested planning artifact, but no source, layout, asset, or configuration file is modified.

- [ ] **Step 2: Verify front matter is unchanged.**

  Run:

  ```bash
  awk 'BEGIN { in_fm=0; count=0 } /^---$/ { count++; if (count == 1) in_fm=1; else if (count == 2) in_fm=0 } in_fm { print }' about.md
  ```

  Expected output is exactly:

  ```text
  layout: ../layouts/PageLayout.astro
  title: "About"
  coverTitle: "关于我"
  date: 2025-01-03 01:01:33
  description: "关于我？"
  ```

  Also verify that the first seven lines match the pre-edit `about.md` snapshot byte-for-byte before accepting the change.

- [ ] **Step 3: Verify the amended embeds and removed legacy identity.**

  Run:

  ```bash
  rg -n 'git-stats|github-readme-streak-stats|lmaodick1239|yusixian|余弦|cosine' about.md
  ```

  Expected: two statistics image URLs are present, one containing `username=lmaodick1239` and one containing `user=lmaodick1239`; no line contains `yusixian`, `余弦`, or `cosine`; and no old badge or old external destination is present.

- [ ] **Step 4: Check required headings and prohibited placeholders.**

  Run:

  ```bash
  rg -n '^#{1,6} |TBD|TODO|FIXME|placeholder|lorem|\[\.\.\.\]' about.md
  ```

  Expected: headings cover identity/summary, capabilities, selected work, and education/achievements; no placeholder marker is present. A contact/links heading must not appear unless independently identity-verified before implementation.

- [ ] **Step 5: Validate Markdown syntax with an available parser.**

  First inspect available project tooling:

  ```bash
  rg -n 'markdown|remark|astro|markdown-it|mistune|commonmark' package.json package-lock.json pnpm-lock.yaml yarn.lock 2>/dev/null || true
  ```

  If an existing Markdown parser command or project check is available, run that repository-supported check against `about.md`. Otherwise use this syntax-focused fallback to confirm front matter delimiters, heading/list structure, and balanced image/link delimiters:

  ```bash
  awk 'BEGIN { d=0 } /^---$/ { d++ } END { if (d != 2) { print "front matter delimiter count: " d; exit 1 } }' about.md
  rg -n '\[[^][]*\]\([^()]+\)|!\[[^][]*\]\([^()]+\)' about.md
  ```

  Expected: exactly two front-matter delimiter lines and validly formed reported Markdown links/images. Do not introduce a dependency solely for this content change.

- [ ] **Step 6: Perform a content acceptance review.**

  Check the rendered/source body manually against the acceptance checklist below: professional English; Edward Chan as the sole prose identity; evidence-grounded claims; correct section order; concise scan-friendly formatting; both amended statistics images retained; no unsupported links, claims, metrics, credentials, current-enrollment claims, or placeholders; and no changes outside `about.md`.

## Content Acceptance Checks

- [ ] The complete front matter, including `PageLayout.astro`, metadata values, delimiter placement, and file type, is unchanged byte-for-byte.
- [ ] The body is professional, direct English and identifies Edward Chan as the sole public identity.
- [ ] The descriptor `IT Operations, Cybersecurity & Automation Assistant` appears with the identity summary.
- [ ] The body follows the approved order: identity and summary; capabilities; selected work; education and achievements.
- [ ] Capability groups are scannable and contain only resume-supported technologies and capabilities.
- [ ] Selected work covers all four approved items and uses evidence-grounded action/outcome wording without invented metrics or impact.
- [ ] Education status uses the resume-supported expected start date `2026-09-01` and does not claim current enrollment or graduation.
- [ ] Splunk BOTS Top 10 in 2025 and Canadian Computing Competition Distinction in 2025 are included accurately.
- [ ] Both existing GitHub statistics image embeds remain in `about.md`; their query values are `username=lmaodick1239` and `user=lmaodick1239` respectively.
- [ ] No `yusixian`, `余弦`, or `cosine` text, old GitHub badge, old profile/streak destination, or unverified contact/project link remains in the body.
- [ ] No unsupported identity or project ownership claim, repository URL, screenshot, demo, download link, user count, performance figure, deployment outcome, business impact, employment claim, certification, award, academic completion, leadership claim, or production-security claim is present.
- [ ] No decorative quote block, marketing callout, second identity, unrelated redesign, or layout change is present.
- [ ] No placeholder text remains, and the Markdown is valid for the existing Astro content pipeline.

## Self-Review Record

- **Spec coverage:** The plan maps the approved scope and front-matter contract to Tasks 1-2 and 4; maps the identity and evidence rules to Global Constraints and Tasks 1-2; maps all four body sections and four selected-work items to Task 2; maps the user amendment for both statistics embeds to Global Constraints and Task 3; maps exclusions and acceptance criteria to Global Constraints, Task 4, and the Content Acceptance Checks.
- **Placeholder scan:** This plan contains no implementation placeholders such as `TBD`, `TODO`, `FIXME`, `lorem`, or `[...]`. The verification command intentionally searches for placeholder markers in the resulting `about.md`.
- **Consistency check:** The only implementation target is `about.md`; the front-matter values, section names, resume facts, image query parameter names (`username` and `user`), and verification expectations are consistent across the file map, tasks, and acceptance checks.
- **Scope check:** This is one Markdown-only content subsystem, so one implementation plan is appropriate. No code, dependency, layout, asset, or configuration work is planned.
